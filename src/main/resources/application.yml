logging:
  level:
    org:

      springframework:
        transaction:
          interceptor: TRACE
        jdbc:
          datasource:
            DatasourceTransactionManager: DEBUG
        orm:
          jpa:
            JpaTransactionManager: DEBUG

      hibernate:
        SQL: DEBUG
        resource:
          transaction: DEBUG


